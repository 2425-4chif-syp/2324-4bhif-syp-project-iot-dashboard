export const environment = {
  production: false,
  // URL of the kiosk frontend. Change this to the actual address where the kiosk app is served.
  // For local development, usually 'http://localhost:4200'
  // For production, use the actual server URL (e.g., 'https://vm67.htl-leonding.ac.at')
  kioskUrl: 'http://localhost:4200'
};